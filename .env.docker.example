# Camera Trap Species Detection Platform - Docker Environment Variables
#
# Copyright (c) 2025 Omar Miranda
# SPDX-License-Identifier: Apache-2.0
#
# Copy this file to .env.docker and update with your values

# Database Configuration
DB_USER=species_admin
DB_PASSWORD=changeme_secure_password
DB_NAME=species_detection
DB_HOST=postgres
DB_PORT=5432

# Database URL for Next.js
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Mapbox (Optional - for map features)
NEXT_PUBLIC_MAPBOX_TOKEN=

# API URL (for client-side requests)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Node Environment
NODE_ENV=production
